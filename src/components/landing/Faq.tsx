import { Accordion, AccordionItem } from '@/components/ui/accordion'
import { useTranslation } from 'react-i18next'

export function Faq() { const { t } = useTranslation(); const questions = [[t('landing.faq.accountQuestion'), t('landing.faq.accountAnswer')], [t('landing.faq.scoringQuestion'), t('landing.faq.scoringAnswer')], [t('landing.faq.gameQuestion'), t('landing.faq.gameAnswer')], [t('landing.faq.shareQuestion'), t('landing.faq.shareAnswer')]]; return <section id="preguntas" className="landing-deferred mx-auto max-w-3xl px-5 py-18"><div className="text-center"><p className="text-xs font-bold tracking-[.18em] text-accent">{t('landing.faq.eyebrow')}</p><h2 className="mt-3 text-3xl font-bold sm:text-4xl">{t('landing.faq.title')}</h2></div><Accordion>{questions.map(([title, answer]) => <AccordionItem key={title} title={title}>{answer}</AccordionItem>)}</Accordion></section> }
